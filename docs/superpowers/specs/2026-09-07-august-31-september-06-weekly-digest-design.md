# 2026-08-31 至 2026-09-06 LLM 推理优化周报设计

## 目标

新增 `2026-09-06.md`，整理 2026-08-31 00:00 至 2026-09-06 23:59（UTC）的 LLM 推理优化动态。周报延续项目已有的信息密度、中文表达和分类统计口径，为推理引擎工程师提供可核验、可用于技术决策的一周摘要。

## 内容结构

1. 本周观察摘要：提炼两至四条贯穿全周的技术主线。
2. vLLM 与 SGLang 合并 PR：全量采集、互斥分类、准确计数，并节选高价值条目。
3. 技术新闻：记录框架版本、重要模型和推理生态动态。
4. 优化技术与论文：覆盖目标时间窗内与推理、serving、kernel、KV cache、量化、并行和投机解码直接相关的工作。
5. 技术趋势：只基于本期已核验材料归纳，并明确区分事实与推断。

## 数据源与时间边界

- 时间窗统一为 2026-08-31 00:00 至 2026-09-06 23:59（UTC）。
- PR、Release 和仓库事件优先使用 GitHub 官方 API 或原始页面。
- 仅统计在时间窗内实际 merged 的 PR；关闭但未合并的 PR 不计入。
- 论文使用 arXiv 摘要页及论文原文，以首次提交日期归档。
- 模型和厂商动态优先使用官方公告、官方博客或官方仓库。
- PR 中的性能、显存和能耗数据均写成作者声明，不表述为独立复现结果。

## PR 采集、分类与节选

分别全量采集 vLLM 和 SGLang 在时间窗内的 merged PR，记录编号、标题、作者、链接、标签和精确 `merged_at`。采集结果须满足记录数等于官方查询总数、编号无重复、全部合并时间处于 UTC 边界内。

分类沿用近期周报，并保证每个 PR 只计入一个主类别：

- vLLM：性能/Kernel、模型/Model Runner、投机解码、KV cache/Offloading/Disaggregation、量化、API/前端/控制面、Bugfix、Docs、Other。
- SGLang：Diffusion、性能/Kernel、调度/并行/分离式服务、投机解码、KV cache/HiCache、模型与多模态、Runtime config、API/框架、Bugfix、Docs、Other。

同一目标的系列 PR 可合并展示，但分类统计仍按独立 PR 计数。正文按跨框架或旗舰模型影响、量化收益、运行时架构变化、生产正确性、一般功能和维护的优先级节选；低影响 CI、依赖和机械维护只做汇总。

## 新闻与论文筛选

新闻只保留在目标时间窗内发布、且对 LLM 推理工程有实质影响的官方 Release、模型发布或基础设施公告。论文主题限定为 LLM inference/serving、KV cache、speculative decoding、quantization、parallelism、attention/MoE kernel 和推理系统；排除与推理没有直接关系的纯训练优化。

每条论文核对标题、首次提交日期、摘要、论文链接和关键实验结论。定量结果明确归因于作者，趋势段不得引入本期材料无法支持的事实。

## 验证与完成标准

- 核对 UTC 日期边界、PR 合并状态、两个仓库的总数和分类加总。
- 检查列出的 PR 在同一仓库内没有非预期重复。
- 检查 Release 版本号、发布日期和论文首次提交日期。
- 检查全部 Markdown 链接、标题层级、中文术语和格式。
- 扫描 `TODO`、`TBD` 等占位文本，并运行 `git diff --check`。
- 仅改动本任务相关文件，不纳入现有未跟踪的 `.claude/`。
- 完成后创建本地提交；任何 `git push` 前单独列出分支、提交和目标 remote，并取得用户明确确认。
