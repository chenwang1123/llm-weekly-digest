# LLM 推理优化周报（2026-09-07 ~ 2026-09-13，UTC）

## 本周观察摘要

本周 vLLM 的主线是 **v0.29.0 发布后，DeepSeek-V4.1-Flash、稀疏 MLA 与 Model Runner V2 的快速收口**。DeepSelect 被接入 DSA sparse indexer（#56464），DSV4.1 的 token/request mapping 与 indexer metadata 被融合成 Triton kernel（#56562），PCP+DCP 扩展到 sparse-MLA（#56157），Engram 则补齐 DP sharding、CPU offload async prefetch 与 node-local shared host table（#56512）。这组工作不只是“支持一个新模型”，而是在同步补齐 sparse top-k、metadata、KV layout、offload 与并行拓扑。

SGLang 的最密集技术簇同样围绕 **DeepSeek-V4.1 + DSpark + Blackwell**。一组 verify/compression/indexer/MoE 融合 PR 把作者报告的 4×GB300 BS1 streamed decode 从 761.03 提升到 853.49 tok/s（#39068），小 batch 专项又从 762.41 提升到 803.25 tok/s（#38976）。与此同时，SGLang 新增 HRRN prefill 调度（#32911）、将 DCP backend 默认解析推广到全部模型（#39165），并继续推进 unified memory、logical-page KV sharding、external cache linker 与 Rust TreeCore，说明性能优化已从单 kernel 扩展到 scheduler、cache ownership 和 runtime configuration pipeline。

KV cache 本周更像一个 **可分片、可迁移、可分层、可验证的数据平面**：vLLM 将 HiSparse host cache 跨 TP rank 共享，并把 PCP producer shard 暴露为 NIXL transfer rank（#56629/#56645）；SGLang 为 UnifiedRadixCache 建立 logical-page placement（#38356），为 Ascend DSA 加入 sparsity-driven host KV offload（#33089），为 Rust TreeCore 接入 external linker（#37306），并落地 FlashMLA V4.1 的 FP8/FP4 paged layouts（#39123）。本周论文也从 CXL 共享 KV tier、离线/在线 MLA reuse、prompt-level adaptive compression 与 RAG KV 拼接等方向印证这一趋势。

正确性方面，最值得关注的不是显式 crash，而是 **长上下文、cache hit 与异步生命周期中的静默错误**。SGLang 修复 Qwen3.8 sparse-decode 未初始化 page tail 导致 NaN 与重复 token 0（#38851），vLLM 修复 NIXL 在读取未完成时回收 remote engine（#54689）、mixed page-size KV offload 注册错位（#54756）和 deferred KV free 引发的 zero-progress preemption cascade（#49675）。这类问题要求回归测试覆盖二次 cache hit、长时间运行、graph replay、跨 batch slot 复用和 transfer teardown，而不只是单请求输出一致性。

> **数据来源与口径**：通过 GitHub 官方 Search API 全量分页枚举 `merged:2026-09-07..2026-09-13`，得到 vLLM **345** 条、SGLang **371** 条合并 PR；逐条校验 PR 编号唯一、`merged_at` 非空且位于目标 UTC 窗口。分类采用互斥主类别，系列 PR 可合并展示但统计仍按独立 PR 计数。Release 来自 GitHub 官方 Releases；论文来自 arXiv 官方分类列表和摘要页，并按首次提交日期过滤。所有性能、显存、容量和能耗数字均为作者声明，**未独立复现**。

---

## Part 1：vLLM & SGLang 上周 PR 汇总

### vllm-project/vllm

> 本周共 **345** 条合并 PR。以下按互斥主类别统计，并按推理系统影响节选。

#### 性能 / Kernel 优化（62 条，节选）

- [#56464 Integrate DeepSelect TopK for the DSA sparse indexer](https://github.com/vllm-project/vllm/pull/56464) — ZJY0516 — 为 SM100/103 接入 DeepSelect，并统一 sparse-indexer top-k backend；作者在 GB200、bs=256、1M KV 上报告 191 µs，对比原 persistent 路径 616 µs。
- [#56562 Fuse DSV4.1 input metadata preparation with Triton](https://github.com/vllm-project/vllm/pull/56562) — WoosukKwon — 合并 token-to-request mapping、varlen indexer expansion 与 padding 初始化；作者报告 DSpark adaptive 的单 token 时间下降 18.20%，高并发吞吐提升较小。
- [#55736 GLM-5.3-Flash decode hot-path cleanup](https://github.com/vllm-project/vllm/pull/55736) — JaredforReal — 去掉 KDA contiguous copies、空 RoPE concat 与重复 router GEMM；作者在 4×GB300 报告 decode throughput 提升约 3%~4%、prefill TTFT 下降 2.5%~2.8%。
- [#56159 Avoid KDA mixed-batch gather/scatter](https://github.com/vllm-project/vllm/pull/56159) — yewentao256 — 以 zero-copy slices 取代每层 6 次 `index_select` 与 2 次 `index_copy_`；作者在 Kimi-K3 8k/1k workload 报告吞吐提升 5.2%~7.7%。
- [#55819 UVA-backed MRV2 `apply_write`](https://github.com/vllm-project/vllm/pull/55819) — bjf-frz — UVA target 直接读取 pinned host contents，避免临时 GPU buffer 与 H2D；作者的同步 microbenchmark 报告 steady-state 最多约 1.96x，但未给出模型端到端收益。

#### 模型支持 / Model Runner（17 条，节选）

- [#56214 DeepSeek-V4.1-Flash model support](https://github.com/vllm-project/vllm/pull/56214) — zyongye — 合入 DeepSeek-V4.1-Flash 执行侧模型定义。
- [#56078 Unify XD-RoPE into M-RoPE](https://github.com/vllm-project/vllm/pull/56078) — hmellor — 统一 rotary 表达并从 config 推导 channel count，减少模型特例。
- [#55355 DeepSeek-V4 CPU backend](https://github.com/vllm-project/vllm/pull/55355) — bigPYJ1151 — 扩展 DeepSeek-V4 的 CPU 执行覆盖。
- [#54774 Cohere Compass](https://github.com/vllm-project/vllm/pull/54774) — am-cohere — 新增 Cohere Compass 模型支持。

#### 投机解码（7 条，节选）

- [#46994 MTP speculative decoding under pipeline parallelism](https://github.com/vllm-project/vllm/pull/46994) — eastwood-c — 补齐 V2 runner 的 MTP+PP build、draft head、embedding loading 与异步 transport 边界。
- [#56107 PCP for single-module MTP and replicated DSpark](https://github.com/vllm-project/vllm/pull/56107) — LucasWilkinson — 将 pipeline context parallel 扩展到两类 draft topology。
- [#55713 NVFP4 DSpark gathered top-k projection](https://github.com/vllm-project/vllm/pull/55713) — askliar — 为 DSpark projection 增加 NVFP4 路径。
- [#56122 Dual-key Gumbel-max watermarking](https://github.com/vllm-project/vllm/pull/56122) — TQCB — 使 watermark sampling 与 speculative decoding 兼容。

#### KV cache / Offloading / Disaggregation（31 条，节选）

- [#56512 DeepSeek-V4.1 Engram offload and DP sharding](https://github.com/vllm-project/vllm/pull/56512) — Juntian777 — Engram table 支持 node-local DP sharding、per-layer async UVA prefetch 与共享 host mapping；把表容量、通信和 compute overlap 放进同一配置面。
- [#54889 Fuse DCP empty-shard LSE mask into A2A pack](https://github.com/vllm-project/vllm/pull/54889) — rbrugaro-amd — 将八个 eager mask kernel 融入 Triton pack；作者报告 24 个 MLA layer 每步净省 0.81 ms，median ITL 下降约 1.85%~2.21%。
- [#56629 Share HiSparse host cache across TP ranks](https://github.com/vllm-project/vllm/pull/56629) / [#56645 expose PCP producer KV shards as transfer ranks](https://github.com/vllm-project/vllm/pull/56645) — LucasWilkinson — 减少 host cache 复制，并显式描述 PCP+DCP 的 transfer topology。
- [#41567 ECMooncakeConnector for encoder cache](https://github.com/vllm-project/vllm/pull/41567) — stmatengss — 通过 Mooncake TransferEngine 传输 EPD encoder cache。
- [#53780 Per-region NIXL transfer geometry](https://github.com/vllm-project/vllm/pull/53780) — MatthewBonanni — 继续把 KV connector 的传输描述从全局布局拆到 region 粒度。

#### 量化（18 条，节选）

- [#54668 H20 block-FP8 MoE low-batch tuning](https://github.com/vllm-project/vllm/pull/54668) — liuyao0322 — 为 GLM-5.3/DeepSeek 共享 shape 调整 tile；作者报告 M=1~8 kernel latency 下降 4.25%~21.11%，未做端到端模型评测。
- [#53319 NVFP4 in the Torch linear backend](https://github.com/vllm-project/vllm/pull/53319) — mlazos — 用 `torch._scaled_mm`、TorchInductor 与现有 layout utility 支持 ModelOpt W4A4 NVFP4，并保留显式 backend 选择。
- [#55356 Group FP8 MLA cache insertion](https://github.com/vllm-project/vllm/pull/55356) — yewentao256 — 将五层 cache insertion 合为一次 grouped launch；作者报告小 batch kernel 级 4~6x。
- [#56688 Avoid redundant FP8 dummy-init conversions](https://github.com/vllm-project/vllm/pull/56688) — WoosukKwon — 减少 FP8 dummy initialization 中的冗余格式转换。

#### API / 前端 / RL 控制面（15 条，节选）

- [#50195 Stateless `/v1/responses/render`](https://github.com/vllm-project/vllm/pull/50195) — franciscojavierarceo — 为 Responses workflow 增加无状态 render endpoint。
- [#55047 Preprocessed multimodal gRPC features](https://github.com/vllm-project/vllm/pull/55047) — biswapanda — Rust frontend 接受已预处理的多模态 feature，降低跨服务重复 preprocessing。
- [#56208 DeepSeek-V4.1 frontend support](https://github.com/vllm-project/vllm/pull/56208) — BugenZhao — 对齐 Rust/Python tokenizer、reasoning/tool parser、grammar 与 image preprocessing；执行侧由独立 PR 承担。
- [#56573 Actual input-token usage for scoring APIs](https://github.com/vllm-project/vllm/pull/56573) — taneem-ibrahim — scoring endpoint 返回实际输入 token 用量。

#### Bugfix（146 条，按生产影响节选）

- [#54689 Protect NIXL engines with in-flight transfers](https://github.com/vllm-project/vllm/pull/54689) — jyizheng — 防止长时间 transfer 超过 TTL 后仍被回收 remote agent，修复无 Python traceback 的 worker 退出。
- [#54756 Register mixed KV page sizes correctly](https://github.com/vllm-project/vllm/pull/54756) — heliubj18 — 按 placement metadata 注册 sparse-MLA 混合 page，而不是用单层 stride 重解释整块 allocation。
- [#49675 Stop zero-progress preemption cascades](https://github.com/vllm-project/vllm/pull/49675) — LearningMachine621 — 修复 deferred KV free 下无进展 preemption 连锁。
- [#55426 Kimi-K3 KDA projection overlap](https://github.com/vllm-project/vllm/pull/55426) — chengchengpei — 修复 Hopper KDA projection 的异步 overlap 正确性。
- [#53945 EAGLE resume Mamba-state position](https://github.com/vllm-project/vllm/pull/53945) / [#55745 PP draft broadcast stream recording](https://github.com/vllm-project/vllm/pull/55745) — 分别修复 recurrent-state checkpoint 与 async PP draft tensor 生命周期。
- [#56478 Odd-row per-token-group quantization cliff](https://github.com/vllm-project/vllm/pull/56478) — ayush1399 — 修复 odd-row 只启动 16 threads 导致的量化性能断崖；作者报告 H100 18 个 production shapes 的 hot/cold 几何平均 latency 分别下降 39.5%/38.0%，尚未重测端到端 TPOT。
- [#56610 ROCm elastic-EP scaling deadlock](https://github.com/vllm-project/vllm/pull/56610) — mawong-amd — 修复 elastic expert parallel 扩缩容死锁。

#### Docs（10 条）与 Other（39 条）

- Docs 主要覆盖新模型、部署 recipe、benchmark 和接口行为；Other 主要为 CI、依赖、测试、typing、build 与内部重构。

**统计**：Perf 62 + Model 17 + Spec 7 + KV 31 + Quant 18 + API 15 + Bugfix 146 + Docs 10 + Other 39 = **345**。

---

### sgl-project/sglang

> 本周共 **371** 条合并 PR。DeepSeek-V4.1/DSpark 是性能主线，统一缓存、runtime config 与 Diffusion serving 并行推进。

#### 扩散模型 / SGLang-Diffusion（41 条，节选）

- [#38584 Qwen-Image-Edit attention on Hopper](https://github.com/sgl-project/sglang/pull/38584) — BBuf — 复用 QKV epilogue 并以 public varlen attention 批量处理 vision windows；作者报告单 H200 lossless eager E2E 下降 1.533%。
- [#39022 Direct-read mapped layers under host-memory pressure](https://github.com/sgl-project/sglang/pull/39022) — mickqian — 当 page cache 无法容纳 mapped component 时，按 tensor residency 决定 memcpy 或 O_DIRECT，面向小内存 desktop 的重复权重流式加载。
- [#36026 Parallel decoder tiling for LTX-2.5](https://github.com/sgl-project/sglang/pull/36026) — AgainstEntropy — 将 decoder tiles 分给 SP ranks；作者在 2×H200、121 frames 上报告 tiling decode 从 5.68s 降至 4.08s。
- [#38529 SANA-WM convolution and streaming GDN](https://github.com/sgl-project/sglang/pull/38529) / [#38530 LongCat normalization and modulation fusion](https://github.com/sgl-project/sglang/pull/38530) — BBuf — 继续压缩 DiT/video denoise 的后处理与 modulation 热路径。
- [#37903 VDN-H3 hybrid attention backend](https://github.com/sgl-project/sglang/pull/37903) — kevin-mii — 为 MiniMax-H3 接入 hybrid-window softmax 与 Video Delta linear attention。

#### 性能 / Kernel 优化（55 条，节选）

- [#39305 Exact BF16 consumer top-k adapted from DeepSelect](https://github.com/sgl-project/sglang/pull/39305) — DarkSharpness — 以两级 radix histogram 替换 DSV4.1 consumer top-512 临时实现，实现 exact selection。
- [#39138 Commit Engram decode history in the hash kernel](https://github.com/sgl-project/sglang/pull/39138) — hnyls2002 — 删除 hash 后五个 torch op，并在 decode graph 内原地推进 n-gram history；作者报告收益较小但结果 bitwise。
- [#36411 Qwen3-VL unique-image serving](https://github.com/sgl-project/sglang/pull/36411) — BBuf — 针对一次性图片流量关闭重型 retention，并去掉 CUDA IPC reconstruction copy；作者报告 68.551 req/s，比同 workload 的历史 vLLM 参考高 11.18%。
- [#38757 Route large-head prefill to Triton unified attention](https://github.com/sgl-project/sglang/pull/38757) — vpremseth23 — AITER 对 `head_dim>256` 不可用时选择 Triton fallback。
- [#31470 FlashInfer MegaMoE](https://github.com/sgl-project/sglang/pull/31470) — wenscarl — 增加 FlashInfer MegaMoE backend 支持。

#### 调度 / 并行 / 分离式服务（19 条，节选）

- [#32911 HRRN scheduling policy](https://github.com/sgl-project/sglang/pull/32911) — SovietPower — 用 uncached input length 估算 service time，并以等待时间避免 SJF starvation；作者在真实 1M-context agent traffic 上报告 mean TTFT -69.09%、p99 -8.30%、input TPS +39.58%。
- [#39165 Model-wide DCP backend defaults](https://github.com/sgl-project/sglang/pull/39165) — kpham-sgl — Blackwell/MNNVL 默认 `fi_a2a`，其他 CUDA/ROCm 默认 `a2a`，并保留显式 `ag_rs` 覆盖；此前默认仅对 Kimi-K3 生效。
- [#38814 Preserve global cache affinity with bucket routing](https://github.com/sgl-project/sglang/pull/38814) — Bo-Vincent — cache winner 在全局 worker pool 选择，bucket 只限制无可用 cache candidate 时的 Power-of-Two fallback。
- [#36651 Qwen3.8-Next PD state transfer](https://github.com/sgl-project/sglang/pull/36651) — YAMY1234 — 为 Flash-Next hybrid state 补齐 prefill/decode transfer。
- [#39122 Gate Responses persistence behind an opt-in](https://github.com/sgl-project/sglang/pull/39122) — ShangmingCai — `--enable-response-store` 默认关闭，并拒绝与 P/D mode 组合，避免无 TTL dict 在 prefill node 上永久增长。

#### 投机解码（22 条，节选）

- [#39068 Fuse DSV4.1 DSpark verify pipeline](https://github.com/sgl-project/sglang/pull/39068) — BBuf — 融合 ratio-2 compression、index selection、RoPE、quantization 与 projection；作者在 4×GB300、BS1 报告 streamed decode +12.15%。
- [#38976 Optimize small DSpark batches](https://github.com/sgl-project/sglang/pull/38976) — BBuf — 增加 split-K WO-A、mHC+RMSNorm fusion 与 MXFP8 draft projection；作者报告 BS1 +5.36%。
- [#38879 DSpark verify and MoE kernels on Blackwell](https://github.com/sgl-project/sglang/pull/38879) — BBuf / DarkSharpness — 将 statistics overlap、router prequantization、finalize/shared-add/all-reduce fusion 带到 DSV4.1。
- [#38949 Static DSpark P/D for DeepSeek-V4.1](https://github.com/sgl-project/sglang/pull/38949) — hnyls2002 — 在 disaggregated deployment 中支持静态 DSpark worker topology。
- [#38748 Online MXFP4 quantization for Qwen3.5 MTP experts](https://github.com/sgl-project/sglang/pull/38748) — zijiecode — AMD 路径在加载阶段将 BF16 draft experts 转为 MXFP4。
- [#37465 gfx950 assembly attention for EAGLE](https://github.com/sgl-project/sglang/pull/37465) — zijiecode — 覆盖 verify、draft extend 与 decode 三类 spec attention phase。

#### KV cache / HiCache / Unified Memory（31 条，节选）

- [#38192 Reland DSV4 unified-KV sizing](https://github.com/sgl-project/sglang/pull/38192) — yuttian1 — 将 SWA/C4 从 token-scaled 改为 request-ring accounting 并完整 gate；作者报告每 DP worker full-token capacity 从 6,848,000 增至 12,569,600（+83.55%）。
- [#33089 Sparsity-driven KV offload on Ascend](https://github.com/sgl-project/sglang/pull/33089) — nzbej — index KV 留在 NPU、full MLA latent KV 放入 registered host memory、device 保留 2,048-token selected cache；作者报告 GLM-5.1 decode 39.42→63.06 TPS。
- [#38356 Logical-page placement](https://github.com/sgl-project/sglang/pull/38356) — Shunkangz — 为后续 KV sharding 建立 stateless ownership bijection 与 per-rank physical page allocator；本 PR 尚未接入 runtime flag。
- [#37306 External cache linker for Rust TreeCore](https://github.com/sgl-project/sglang/pull/37306) — Jialin — 把 Full 与 Full+SWA linker state machine 从 Python node object 迁移到 backend-neutral/Rust `NodeId` 接口。
- [#38827 Ascend Memcache HiCache L3](https://github.com/sgl-project/sglang/pull/38827) — jamesjxliu — 新增 Ascend Memcache 作为 HiCache L3 storage backend。
- [#39123 DeepSeek-V4.1 paged FP8/FP4 KV layouts](https://github.com/sgl-project/sglang/pull/39123) — DarkSharpness — 为 FlashMLA V4.1 描述压缩 latent 的 FP8/FP4 page bytes 与 dequant workspace。
- [#37584 SWA branching-point caching in Rust TreeCore](https://github.com/sgl-project/sglang/pull/37584) — alphabetc1 — 修复 Python backend 已支持、Rust backend 却静默 no-op 的 feature parity 缺口。

#### 模型与多模态支持（24 条，节选）

- [#37500 Qwen3.8-Flash-Next](https://github.com/sgl-project/sglang/pull/37500) — Qiaolin-Yu — 合入 Qwen3.8-Flash-Next 模型支持。
- [#38621 GLM-5.3-Flash NVFP4 loading](https://github.com/sgl-project/sglang/pull/38621) — nvpohanh — 支持 GLM-5.3-Flash NVFP4 checkpoint loading。
- [#35599 NemotronH Omni Reasoning V3](https://github.com/sgl-project/sglang/pull/35599) — rystewart-nvidia — 新增 NemotronH Omni Reasoning V3。
- [#34556 Mamba 1/2 inference](https://github.com/sgl-project/sglang/pull/34556) — desmond-intel — 扩展 Mamba 1 与 Mamba 2 推理支持。

#### Runtime config / 状态解耦（9 条）

- [#38046](https://github.com/sgl-project/sglang/pull/38046) / [#38047](https://github.com/sgl-project/sglang/pull/38047) / [#38048](https://github.com/sgl-project/sglang/pull/38048) / [#38049](https://github.com/sgl-project/sglang/pull/38049) / [#38752](https://github.com/sgl-project/sglang/pull/38752) / [#38753](https://github.com/sgl-project/sglang/pull/38753) / [#39134](https://github.com/sgl-project/sglang/pull/39134) — ch-wan — 持续把 declaration、requested state、resolution、sealed record 与 runtime read view 拆开，并为 resolution pipeline 的每一步提供 out-of-tree replacement point。

#### API / 工具 / 框架（6 条）

- [#38690 Responses API extensions](https://github.com/sgl-project/sglang/pull/38690) — Dovis01 — 支持 custom tools、encrypted reasoning replay、developer tier 与 model validation。
- [#38951 DeepSeek-V4.1 structural tool-call tags](https://github.com/sgl-project/sglang/pull/38951) — Oasis-Git — 按 DSML spaced names 构造 tool-call structural tag，补齐新模型的 parser/API 兼容性。

#### Bugfix（93 条，按生产影响节选）

- [#38851 Make QSA paged sparse-decode gather memory-safe](https://github.com/sgl-project/sglang/pull/38851) — YAMY1234 — zero-fill unwritten page tail、使用 int64 offsets 并在 gather 时正确 dequant FP8，修复长请求 NaN、重复 token 0 与跨 batch-slot 污染。
- [#38596 Account KV-canary workspace after graph capture](https://github.com/sgl-project/sglang/pull/38596) — cctry — post-capture KV sizing 预留 target/draft canary 最大 workspace，避免大 KV pool 配置启动后接近 1 GiB 的未计费 workspace 触发 OOM。
- [#33922 Qwen3.5 GDN multi-item scoring](https://github.com/sgl-project/sglang/pull/33922) — daii-0818 — 修复多 item scoring 下 recurrent state/输出错误。
- [#38963 DeepSeek-V4.1 VL fused shared expert routing](https://github.com/sgl-project/sglang/pull/38963) — rchalamala — 修复 VL routing 丢弃 fused shared expert。
- [#37564 AITER bpreshuffle GEMM dispatch](https://github.com/sgl-project/sglang/pull/37564) — yichiche — 避免 Qwen3.5 MXFP attention FP8 TP4 选择无法处理 output shape 的 GEMM。

#### Docs（18 条）与 Other（53 条）

- Docs 覆盖 DeepSeek-V4.1、GLM-5.3、Qwen3.8、Kimi-K3、Diffusion 与多硬件 cookbook；Other 主要为 CI、依赖、release backport、镜像、测试和内部重构。

**统计**：Diffusion 41 + Perf 55 + Sched 19 + Spec 22 + KV 31 + Model 24 + Config 9 + API 6 + Bugfix 93 + Docs 18 + Other 53 = **371**。

---

## Part 2：上周大模型推理优化技术新闻

### 框架发布与版本更新

- **[vLLM v0.29.0](https://github.com/vllm-project/vllm/releases/tag/v0.29.0)** — 9 月 9 日发布，官方 release notes 统计 594 commits、277 contributors。MRV2 成为所有模型的默认 runner；新增 Hy4-preview、Qwen3.8-Flash-Next、GraniteSWA、NemotronH Omni Reasoning V3 与 Kimi-K3 NVFP4，并集中交付 Kimi-K3/DSV4 kernel、speculative decoding、Mamba prefix caching、RL weight sync、context parallelism 与 KV connector 能力。
- **v0.29.0 升级注意事项** — MRV1 被标记 deprecated，官方目标是在 v0.32 移除；FlashInfer all-reduce 对 CUDA TP group 默认启用；`NONE_HASH` prefix-cache hash 改为 deterministic；新增 queue admission limits；同时删除十个旧 architecture、PyAV video backend 和若干环境变量，并将 `python -m vllm.entrypoints.openai.api_server` 标记 deprecated。升级前应重跑 runner fallback、all-reduce、video decode、hybrid prefix retention 与自定义 launcher 测试。
- **SGLang 本周无新 release tag** — main 在 v0.5.19 之后继续高速吸收 DeepSeek-V4.1、Qwen3.8、GLM-5.3、DSpark、unified cache、RuntimeContext/config pipeline 与 Diffusion serving 工作。

### 模型与推理生态

- **DeepSeek-V4.1-Flash 形成跨框架完整部署面** — vLLM 同周合入 model/frontend、DeepSelect、Engram、PCP+DCP、FP4/FP8 与 metadata fusion；SGLang 同步补齐 paged KV layout、DSpark verify、P/D、Blackwell/ROCm/NPU backend 和 cookbook。工程竞争点已从“能加载”推进到 sparse indexer、draft acceptance、cache geometry 与 cold-start/runtime defaults。
- **DSA top-k 出现可移植 kernel 竞争** — vLLM 将 DeepSelect 封装为可选 sparse-indexer backend（#56464），SGLang 则把算法中的 exact BF16 consumer selection 专门化进现有 kernel（#39305）。同一算法分别以 library integration 与 in-tree specialization 落地，后续比较应覆盖 build portability、dispatch heuristic、CUDA graph 与真实 context distribution，而不只看单 shape microbenchmark。
- **异构低精度的重心转向“布局 + dispatch + fallback”** — 本周 NVFP4 Torch backend、H20 FP8 MoE tune、Qwen3.5 MTP online MXFP4、FlashMLA V4.1 FP4/FP8 page layout 与 AITER fallback 同时推进；决定可部署性的已不只是算术格式，而是 checkpoint alias、scale layout、page bytes、backend eligibility 与失败时是否安全回退。

### 优化技术与论文（2026-09-07 ~ 2026-09-13）

- **[RedKnot-MLA：DeepSeek-V4 长上下文的离线/在线复用](https://arxiv.org/abs/2609.07008)** — 9 月 7 日首投；文档在 canonical position 预计算可复用 Local-head contribution，在线重算 Global heads 与保护 token rows，再在 shared output projection 前合并。作者报告 hot-artifact TTFT 2.02~3.84x，并明确标注部分 256K QPS 为 preliminary。
- **[Online Draft Co-Training：长上下文 RL rollout 的在线 draft 共训](https://arxiv.org/abs/2609.07108)** — 9 月 7 日首投；以 branch-aware zigzag ring attention 处理 CP，以独立 TapChannel 跨 PP stage 传输 target features，覆盖最高 122B 与 256K context。
- **[MetaKV：按 prompt 与预算选择 KV compression](https://arxiv.org/abs/2609.07966)** — 9 月 7 日首投；为 KVQuant/H2O/RocketKV/FP16 候选预测 latency、peak memory 与正确概率，在 user constraints 下逐请求选配置；作者报告 constrained success rate 平均 +0.07、最高 +0.135。
- **[Epoch：把 diffusion block 编译成 Sparse-MoE serving unit](https://arxiv.org/abs/2609.09748)** — 9 月 9 日首投；跨 refinement iterations 维护 block plan，只让 live/refresh-required token-expert work 进入 fresh EP collectives；作者在 8×H100、7B~100B block-diffusion MoE 上报告 E2E 最多 2.7x。
- **[KV concatenation-aware RAG](https://arxiv.org/abs/2609.09768)** — 9 月 9 日首投；联合训练模型适应预计算 KV 拼接，并选择性重算部分 cache；作者在 124K RULER 上报告相对仅重算 baseline +9.7 points，同时对比 full attention 的 TTFT -80%。
- **[Composable CXL memory as a shared KV tier](https://arxiv.org/abs/2609.10790)** — 9 月 9 日首投；Kubernetes DRA 将 CXL region 注入跨节点 pods，vLLM/llm-d connector 直接把 slot directory 放在共享介质中。作者在双节点 512 GiB CXL/Qwen2.5-7B 上报告 cross-node prefix reuse TTFT 5.5~36.6x，并将结论限定为 feasibility study。
- **[ExaServe：在 exascale HPC 部署大规模 LLM serving](https://arxiv.org/abs/2609.10812)** — 9 月 9 日首投；把 scheduler、MPI launch、accelerator selection 与 node-local weight staging 封装为 YAML deployment。在 Aurora 上扩展到 256 nodes/3,072 vLLM replicas，作者报告 non-streaming 27.1k req/s，同时暴露 centralized streaming proxy 与 Ray Serve O(N²) control-plane 瓶颈。
- **[Phase-decoupled power control for P/D serving](https://arxiv.org/abs/2609.11133)** — 9 月 10 日首投；prefill 用 SM-clock window，decode 用模型校准的 power cap，并以 runtime SLO guard 约束。在 8×B200、Qwen3-Coder-480B FP8 上，作者报告 balanced mode tokens/J +20.4%、mean E2E +3.5%，优于 vendor Max-Q 的 +8.6%/+5.2%。
- **[REVA：复用历史 attention 构建 RAG evidence views](https://arxiv.org/abs/2609.11209)** — 9 月 10 日首投；把 query-document-model 历史聚合为 document-keyed、budget-agnostic score store，在线输出保持原顺序的 plain-text views。作者报告质量 +1.0~5.8 points、compression overhead 降 5.3~15.6x、额外 latency <40 ms。
- **[HeatCache：液冷 edge server 的热感知调度](https://arxiv.org/abs/2609.12449)** — 9 月 11 日首投；把 chassis AIO loop 视为临时 heat buffer，在 thermal safety 与 SLO 约束下调度。作者在 vLLM 实现上报告 compute energy 最多 -18.0%、thermal-throttle exposure -81.7%、SLO violation <0.9%。
- **[RoofLang：面向 LLM inference architecture search 的 DSL](https://arxiv.org/abs/2609.12551)** — 9 月 11 日首投；提供 workload representation、可验证 mutation space 与 implementation-independent evaluator。作者称 persistent optimizer agent 在 B300 上为 DeepSeek-V4 Pro 找到吞吐与交互性同时改善 6.23%~50.1% 的架构。
- **[Dissecting GPU Utilization on Hopper](https://arxiv.org/abs/2609.12923)** — 9 月 11 日首投；以 vLLM、FA3 与 cuBLASLt 的 NCU raw counters 将单一 utilization 拆为 fragment fill、occupancy、stall、wave quantization 与 kernel selection 八类视图，强调 decode 小 M GEMM 的“高利用率”不等于高有效工作比例。

### 从本周数据提炼的技术趋势

- **Sparse attention 的端到端路径开始成为新主战场**：top-k、metadata、compressed KV、page layout、DCP transport、draft verify 与 MoE routing 必须协同；只优化 attention 主 kernel 已不足以兑现模型级收益。
- **KV cache 的接口正在从 tensor 指针升级为 topology-aware object**：logical page、transfer rank、region geometry、external linker、CXL tier、cross-model/offline reuse 和 adaptive compression 都要求显式表达 ownership、layout、lifecycle 与 consistency。
- **CPU/control plane 重新暴露为尾延迟上限**：vLLM EPD serialization、SGLang HRRN/router、metadata torch op chains、ExaServe centralized proxy 与 Ray control plane 都说明 GPU kernel 越快，Python/Rust proxy、launch count、admission 与 orchestration 越需要单独测量。
- **推理优化开始把热、电与主机资源纳入 SLO controller**：phase-decoupled power cap、HeatCache 与 host-CPU interference 共同表明，生产调优应按 prefill/decode lane、模型量化组合和具体机箱环境校准，而不是套用单一 GPU profile。
- **正确性回归需要“状态化 workload”**：QSA page tail、NIXL teardown、SWA branching cache、Mamba/EAGLE resume 和 mixed page-size registration 的问题都依赖长时间运行或二次复用；测试矩阵应显式覆盖 cache hit/miss 转换、graph replay、batch-slot reuse、abort 与异步 cleanup。
